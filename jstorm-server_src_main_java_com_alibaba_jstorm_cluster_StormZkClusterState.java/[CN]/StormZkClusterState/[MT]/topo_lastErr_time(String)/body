{
  String path=Cluster.lasterror_path(topologyId);
  Map<Integer,String> lastErrTime;
  lastErrTime=(Map<Integer,String>)(Cluster.maybe_deserialize(cluster_state.get_data(path,false)));
  return lastErrTime;
}
