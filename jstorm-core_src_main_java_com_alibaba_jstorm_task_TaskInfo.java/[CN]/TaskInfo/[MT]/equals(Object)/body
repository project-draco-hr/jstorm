{
  if (assignment instanceof TaskInfo && ((TaskInfo)assignment).getComponentId().equals(getComponentId()) && ((TaskInfo)assignment).getComponentType().equals(componentType)) {
    return true;
  }
  return false;
}
