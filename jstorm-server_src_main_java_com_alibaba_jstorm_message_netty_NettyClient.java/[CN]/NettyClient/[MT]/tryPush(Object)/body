{
  if (useDisruptor) {
    try {
      disruptorQueue.tryPublish(obj);
    }
 catch (    InsufficientCapacityException e) {
      return false;
    }
    return true;
  }
 else {
    return blockingQueue.offer(obj);
  }
}
