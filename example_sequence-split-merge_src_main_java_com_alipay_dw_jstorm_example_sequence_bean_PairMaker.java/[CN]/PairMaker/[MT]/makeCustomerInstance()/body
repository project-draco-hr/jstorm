{
  Pair pair=new Pair();
  String key=customerType[rand.nextInt(customerType.length)];
  Long value=(long)((rand.nextInt(1000000) - 200000) / 1000);
  pair.setKey(key);
  pair.setValue(value);
  return pair;
}
