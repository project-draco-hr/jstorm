{
  if (val == null) {
    return;
  }
  try {
    TupleInfo tupleInfo=(TupleInfo)val;
    event_queue.offer(new FailSpoutMsg(spout,(TupleInfo)tupleInfo,task_stats,isDebug));
  }
 catch (  Exception e) {
    LOG.error("expire error",e);
  }
}
