{
  if (isLimited == false) {
    emit();
    return;
  }
  if (isFinished == true) {
    LOG.info("Finish sending ");
    return;
  }
  if (tupleId > SPOUT_MAX_SEND_NUM) {
    isFinished=true;
    return;
  }
  emit();
}
