{
  conf=UIUtils.readUiConfig();
  port=ConfigExtension.getDeamonHttpserverPort(conf);
  try {
    generateLogFileName();
    queryLog();
  }
 catch (  Exception e) {
    LOG.error(e.getCause(),e);
    throw e;
  }
}
