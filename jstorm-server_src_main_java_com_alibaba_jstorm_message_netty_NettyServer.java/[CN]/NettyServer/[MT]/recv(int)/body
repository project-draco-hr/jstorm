{
  try {
    if ((flags & 0x01) == 0x01) {
      return (TaskMessage)recvQueue.poll();
    }
 else {
      return (TaskMessage)recvQueue.take();
    }
  }
 catch (  Exception e) {
    LOG.warn("Occur unexception ",e);
    return null;
  }
}
