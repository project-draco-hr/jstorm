{
  StringBuilder sb=new StringBuilder();
  sb.append(clusterName).append(MetricUtils.AT).append(topologyId).append(MetricUtils.AT).append(taskId).append(MetricUtils.AT).append(host).append(MetricUtils.AT).append(port);
  return sb.toString();
}
