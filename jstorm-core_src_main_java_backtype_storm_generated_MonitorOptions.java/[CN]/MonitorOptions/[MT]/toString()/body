{
  StringBuilder sb=new StringBuilder("MonitorOptions(");
  boolean first=true;
  if (is_set_isEnable()) {
    sb.append("isEnable:");
    sb.append(this.isEnable);
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
