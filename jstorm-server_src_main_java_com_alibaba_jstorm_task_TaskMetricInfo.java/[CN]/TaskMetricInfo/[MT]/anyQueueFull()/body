{
  String ret=null;
  if (gaugeData.get(MetricDef.DESERIALIZE_QUEUE) == FULL_RATIO || gaugeData.get(MetricDef.SERIALIZE_QUEUE) == FULL_RATIO || gaugeData.get(MetricDef.EXECUTE_QUEUE) == FULL_RATIO) {
    ret=component + "-" + taskId+ ": queue is full";
  }
  return ret;
}
