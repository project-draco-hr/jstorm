{
  if (args.length >= 1) {
    if (args[0].equals("rpc")) {
      SetDPRCTopology();
      return;
    }
 else     if (args[0].equals("local")) {
      SetLocalTopology();
      return;
    }
    LoadProperty(args[0]);
  }
  SetRemoteTopology();
}
