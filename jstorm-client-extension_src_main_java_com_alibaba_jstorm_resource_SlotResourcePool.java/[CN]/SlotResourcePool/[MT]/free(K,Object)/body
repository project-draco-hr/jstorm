{
  if (slot == null) {
    return;
  }
  LOG.debug(context + " free " + type+ ":"+ slot);
synchronized (type) {
    Boolean value=slots.get(slot);
    if (value == null) {
      return;
    }
    slots.put(slot,Boolean.valueOf(false));
  }
  return;
}
