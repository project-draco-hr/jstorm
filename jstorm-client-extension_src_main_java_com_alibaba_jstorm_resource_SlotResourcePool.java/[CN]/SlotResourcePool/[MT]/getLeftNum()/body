{
  int freeSlotNum=0;
synchronized (type) {
    for (    Entry<K,Boolean> entry : slots.entrySet()) {
      if (entry.getValue() == true) {
        continue;
      }
      freeSlotNum++;
    }
  }
  return freeSlotNum;
}
