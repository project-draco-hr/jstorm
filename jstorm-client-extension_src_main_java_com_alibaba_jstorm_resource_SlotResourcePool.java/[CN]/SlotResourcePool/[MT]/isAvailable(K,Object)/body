{
  if (slot == null) {
    return true;
  }
synchronized (type) {
    Boolean value=slots.get(slot);
    if (value == null) {
      return false;
    }
    if (value == true) {
      LOG.debug(type + " slot " + slot+ " is assigned,"+ context);
      return false;
    }
    return true;
  }
}
