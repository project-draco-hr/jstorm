{
  if (obj instanceof SlotResourcePool<?> == false) {
    return false;
  }
  SlotResourcePool<K> otherPool=(SlotResourcePool<K>)obj;
  return otherPool.getType().equals(type) && otherPool.slots.equals(slots);
}
