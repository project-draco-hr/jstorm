{
  StringBuilder sb=new StringBuilder("MetricInfo(");
  boolean first=true;
  if (is_set_metrics()) {
    sb.append("metrics:");
    if (this.metrics == null) {
      sb.append("null");
    }
 else {
      sb.append(this.metrics);
    }
    first=false;
  }
  sb.append(")");
  return sb.toString();
}
