{
  if (p1 == null || p2 == null) {
    return null;
  }
 else {
    int p1Size=p1.size();
    int p2Size=p2.size();
    if (p1Size != p2Size) {
      return null;
    }
 else {
      List<Double> rtn=new ArrayList<Double>();
      for (int i=0; i < p1Size; i++) {
        rtn.set(i,p1.get(i) + p2.get(i));
      }
      return rtn;
    }
  }
}
