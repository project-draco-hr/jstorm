{
  if (!getClass().equals(other.getClass())) {
    return getClass().getName().compareTo(other.getClass().getName());
  }
  int lastComparison=0;
  lastComparison=Boolean.valueOf(is_set_uploadMetrics()).compareTo(other.is_set_uploadMetrics());
  if (lastComparison != 0) {
    return lastComparison;
  }
  if (is_set_uploadMetrics()) {
    lastComparison=org.apache.thrift.TBaseHelper.compareTo(this.uploadMetrics,other.uploadMetrics);
    if (lastComparison != 0) {
      return lastComparison;
    }
  }
  return 0;
}
